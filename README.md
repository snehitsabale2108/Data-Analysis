# Data-Analysis
Data Analyzer is a Python desktop app (Tkinter + Pandas + Matplotlib) that lets users load CSV/Excel files, build grouped summary reports with custom aggregations, visualize them as charts, and export both report and chart
